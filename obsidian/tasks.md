# vehicle_cost_tracker — задачі та статус

Останнє оновлення: 2026-08-24.

## Зроблено (за git log / CODING_GUIDE.md)

- **Фаза 1-4** — ініціалізація проєкту, типи, лендінг (`TopNav`,
  `AuthModal`, `LandingPage`/`UnderConstruction`), git/Docker/GitHub
  Actions автодеплой на warehouse.mom.
- **Фаза 4.5 — ПОВНІСТЮ ЗАВЕРШЕНА, задеплоєна**: логін/реєстрація/вихід
  через Django-сесію.
- **Фаза 5-9** — утиліти (включно з `parseQR.ts` — парсер QR ESP/OPT/
  Rubin, готовий, ще без UI камери), API-шар, React Query hooks,
  ui/-компоненти, layouts (`DriverLayout`, `MainLayout`).
- **Фаза 10-11** — `App.tsx`/маршрутизація, перша реальна сторінка
  `WaybillList`.
- **Фаза 13 — DriverDashboard/EventForm — набрано й перевірено наскрізь**
  проти реального Django API (не mock): картка авто, перемикач
  daily/full режиму, тайли типів подій, форма з умовними полями під
  тип, збереження `RouteEvent`.
- **Telegram-реєстрація водіїв** (бекенд `vehicle_tracker_api`) —
  задеплоєна й робоча в проді: бот (aiogram), Mini App логін
  (`initData`), підтвердження заявки з вибором ролі прямо в Telegram.
  `/driver-app` (`DriverMiniApp.tsx`) підключений. Деталі —
  `TELEGRAM_BOT_SETUP.md` у бекенд-репо.
- **2026-08-10 (бекенд-репо):** знайдено й виправлено, що коміти
  Фази 11 двічі губили `/driver-app` route і весь `src/api/cars.ts`
  (119 рядків) — деплої фронтенду мовчки падали з 09.08. Відновлено,
  позначено коментарем `⚠️ НЕ ВИДАЛЯТИ`.
- **2026-08-11 (ця сесія):**
  - `src/api/cars.ts` — знайдено й прибрано повний дублікат вмісту
    файлу (рядки 1-119 продубльовані в 120-239, зламало б `tsc`) —
    після видалення файл повністю збігся з версією в `main`, комітити
    не було чого.
  - **Обидва гайди звірені з фактичним кодом і виправлені**:
    - `CODING_GUIDE.md`: `MainLayout` (Крок 9.3) — код давно темний
      градієнт, гайд досі показував світлу `bg-gray-50` версію;
      виправлено. Крок 13.6 — гайд стверджував "`/` лишається
      `LandingPage`", реальний код досі має `Navigate` на `/driver` —
      позначено як розходження, вирішується Фазою 14.
    - `DJANGO_CODING_GUIDE.md`: `Driver.telegram_id` прибрано з гайду
      (у коді давно `Profile.telegram_id`, `Driver.drivers_license`
      додано в гайд, бо реально є в моделі).
  - **Реальний баг знайдено й виправлено в коді** (не лише в гайді):
    `apps/cars/serializers.py::MonthlyCostsSerializer` мав
    `other_cost_uah`/`other_cost_comment` (однина) замість
    `other_costs_uah`/`other_costs_comment` (множина, як в моделі) —
    зламало б серіалізатор при першому зверненні. Виправлено, `python
    manage.py check` проходить чисто.
  - **Гайди доповнені новими фазами** (тільки текст гайду — код ще НЕ
    набраний, це наступний крок):
    - `DJANGO_CODING_GUIDE.md` Фаза 9 — ролі/права доступу для
      автопарку (зараз усі `/api/cars/`, `/api/drivers/` тощо відкриті
      без логіну — `DEFAULT_PERMISSION_CLASSES` не задано), Фаза 10 —
      записувані `specs`/`trailer` в `CarSerializer` (nested write),
      щоб логіст міг завести авто з характеристиками однією формою.
    - `CODING_GUIDE.md` Фаза 14 — рольова маршрутизація (`RequireRole`,
      `RoleRedirect`, `/` повертається на `LandingPage`, після логіну
      водій → `/driver`, logist/manager/head → `/fleet`), Фаза 15 —
      `QRScanner` (html5-qrcode) + вбудовування в `EventForm` для
      `delivery`-подій, реальна `/driver/history`, Фаза 16 — CRUD
      автопарку для логіста (`FleetList`, `CarForm`, `createCar`/
      `updateCar`/`deleteCar` в `src/api/cars.ts`).
- **2026-08-16 (коміт `d82b5eb`) — Mini App редіректить за роллю, не
  лише на `/driver`**: `DriverMiniApp.tsx` після успішного логіну через
  `initData` тепер сам обирає посадку за `ROLE_LANDING`
  (`driver`→`/driver`, `logist`/`manager`/`head`→`/fleet`) — раніше
  вела на `/driver` для всіх ролей. Це вирішує лише вхід через Telegram
  Mini App; захисту самих маршрутів (`RequireRole` — щоб хтось із
  роллю `driver`, зайшовши напряму в URL `/fleet`, туди не потрапив)
  усе ще немає — це і є Фаза 14.
- **2026-08-16 (коміт `206b595`) — виправлено хибний запис у
  `CODING_GUIDE.md`**: хтось раніше помилково позначив Фази 15/16 як
  "виконано", хоча жоден файл (`RequireRole.tsx`, `QRScanner.tsx`,
  `FleetList.tsx`, `CarForm.tsx`) реально не набраний — перевірено
  повторно і зараз (2026-08-24), досі так: `src/components/fleet/`,
  `src/pages/fleet/` порожні, `/fleet` у `App.tsx` рендерить
  `PlaceholderPage`.
- **2026-08-24 (ця сесія) — повний ресинк `documents/01-08`**: усі 8
  файлів ТЗ/специфікації приведені у відповідність до фактичного коду
  (кожен розділ позначено ✅/⏳), виправлено застаріле "бекенд поза
  MVP" (авторизація вже жива), додано опис `/driver-app` (Mini App) у
  `04_PAGES_AND_ROUTING.md`, реальне дерево `src/` у
  `08_PROJECT_STRUCTURE.md`. Побічна знахідка: `LandingPage.tsx`/
  `UnderConstruction.tsx` повністю готові, але `App.tsx` їх ніде не
  імпортує — `/` як і раніше `Navigate` на `/driver` для всіх
  (виправляється тією ж Фазою 14). Також знайдено недокументований
  env-var `VITE_TELEGRAM_BOT_USERNAME` (використовується в
  `AuthModal.tsx`/`DriverMiniApp.tsx`) — доданий в `env.example.md`.

## Наступний крок

> ⚠️ **Перевірено повторно 2026-08-26 — бекенд пішов значно вперед,
> нижче застарілий запис від 2026-08-11.** `apps/cars/views.py` більше
> НЕ відкритий без авторизації: `get_permissions()` тепер повертає
> `[IsAuthenticated(), IsLogistOrAbove()]` на запис і `[IsAuthenticated()]`
> на читання (перевірено напряму в коді). Бекенд-Фаза 9 (ролі/права) і
> Фаза 10 (Fleet CRUD nested write) — **готові й змержені** (`git log`
> бекенду: `5364f32` PR #1, `6df8ac5`/`2da211f` PR #2). Фаза 11
> (`apps/logistics` — найманий транспорт + служби доставки) — моделі
> закомічені (`453d461`), а серіалізатори/views/urls/admin + міграція
> `0002` лежать у робочій копії бекенду ЩЕ НЕ закомічені (`git status`
> бекенду: `apps/logistics/{admin,models,serializers,urls,views}.py`
> modified/added, `config/urls.py` вже підключає `apps.logistics.urls`).
> Деталі — `STATE.md`/`CHANGES.md` цього репо (записи 2026-08-26).

Тому актуальний порядок:

1. **Фронтенд Фаза 14 (`RequireRole`)** — саме з цього продовжувати.
   Це чисто фронтенд-робота, не залежить від бекенду, і саме вона
   розблоковує тестування решти через UI (без неї нема способу зайти
   під потрібною роллю через веб, лише через Telegram Mini App).
2. **Фронтенд Фаза 16 (`FleetList`/`CarForm`)** — бекенд для неї вже
   повністю готовий (Фаза 9+10 змержені), можна одразу після Фази 14,
   не чекаючи на бекенд.
3. **Фронтенд Фаза 15** (QR-сканер водія) — незалежна, у будь-якому
   порядку відносно Фази 16.
4. Коли бекендова Фаза 11 буде закомічена — `/hired`/`/carriers`
   сторінки фронтенду (Крок 12-13 "Що далі" `CODING_GUIDE.md`) можна
   робити раніше, ніж вважалось: бекенд для них уже написаний і
   перевірений (`manage.py check` чистий), лишається лише закомітити
   й протестувати через залогинену сесію.
   після Фази 14.

```bash
git checkout main
git pull origin main
git checkout -b feature/faza-14-role-routing
```

Далі за старим планом (не змінилось): `WaybillDetail`, `HiredTripForm`,
`CarrierShipmentForm`, аналітика/графіки (Recharts), адмін-панель
підтвердження реєстрацій на `/panel` (тепер "Крок 15" у "Що далі"
`CODING_GUIDE.md`, було "Крок 18").

## Відкриті питання

- `package-lock.json` — досі дрібні локальні розходження, не мої,
  не займав жодного разу.
- Інший процес комітить/мержить у `main` незалежно від цієї сесії —
  перед плануванням нової гілки звіряй `git log`.
