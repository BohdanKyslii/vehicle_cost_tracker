# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.production

# Excel/CSV source data (real business data, stays local - see documents/)
# Recursive (**) — a previous plain "documents/*.xlsx" missed files added
# under a subfolder like documents/wehicle_tracker/, which got git-added
# by mistake (caught 2026-09-09) before this fix.
documents/**/*.xls
documents/**/*.xlsx
documents/**/*.csv
documents/file_1C/
